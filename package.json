{
  "name": "omideno7-meetings-react",
  "private": true,
  "version": "0.18.1-step18-deploy-fix",
  "type": "module",
  "description": "OmideNo7 Meetings — Vite + React + TypeScript PWA test version",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint:types": "tsc --noEmit",
    "pwa:check": "echo 'Check manifest.webmanifest, sw.js, icons and install page'",
    "build:pwa": "npm run build",
    "deploy:build": "npm run build",
    "deploy:preview": "npm run build && npm run preview",
    "deploy:vercel": "npm run build && echo 'Upload or connect this project to Vercel'",
    "deploy:netlify": "npm run build && echo 'Deploy dist folder to Netlify'",
    "deploy:github-pages": "npm run build && echo 'Use .github/workflows/deploy-github-pages.yml'"
  },
  "dependencies": {
    "@supabase/supabase-js": "latest",
    "livekit-client": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "@vitejs/plugin-react": "latest",
    "typescript": "latest",
    "vite": "latest"
  },
  "appStage": "step-18-deploy-test-version"
}
