# Step 16B — Next Backend Tasks

After the React shell is stable:

1. Connect Supabase Auth.
2. Replace mock profiles with `profiles` table.
3. Replace mock access requests with `access_requests`.
4. Replace mock meetings with `meetings`.
5. Add Supabase RLS tests.
6. Build secure LiveKit token Edge Function.
7. Connect Waiting Room admission to token endpoint.
8. Connect recordings and reports.
