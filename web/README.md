# Web README

Simple React (Vite) scaffold to start the web app. This app expects the backend to be available under /api (proxy or same domain).

Local dev (example):
1. cd web
2. npm install
3. npm run dev

You may add a vite.config.js proxy to forward /api to local backend:3000
