# Web README

This folder will contain the React (TypeScript) web application for OmideNo7 Meetings (user + admin). Pages planned:
- Login
- RequestAccess
- PendingApproval
- WaitingRoom
- Meeting
- AdminDashboard

Current status: scaffold placeholder. Implementation will be committed in follow-up commits.

Run locally (planned):
- `npm install` then `npm start`

